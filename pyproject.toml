[project]
name = "ui-autotests"
version = "0.1.0"
description = ""
authors = [
    {name = "nLightRU",email = "northenlight6899@gmail.com"}
]
license = "MIT"
requires-python = ">=3.12"
dependencies = [
    "pytest (>=9.0.3,<10.0.0)",
    "playwright (>=1.60.0,<2.0.0)",
    "pytest-playwright (>=0.8.0,<0.9.0)",
    "allure-pytest (>=2.16.0,<3.0.0)",
    "python-dotenv (>=1.2.2,<2.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "black (>=26.5.1,<27.0.0)",
    "pylint (>=4.0.5,<5.0.0)"
]
